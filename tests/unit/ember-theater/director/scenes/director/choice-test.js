import directorChoice from 'ember-theater-web/ember-theater/director/scenes/director/choice';
import { module, test } from 'qunit';

module('Unit | EmberTheaterScene | director/choice');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = directorChoice();
  assert.ok(result);
});
